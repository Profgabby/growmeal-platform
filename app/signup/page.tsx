'use client';
import Link from 'next/link';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Signup(){const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');async function go(e:any){e.preventDefault();setMsg('Creating account…');const s=createClient();const {data,error}=await s.auth.signUp({email,password,options:{data:{full_name:name}}});if(error){setMsg(error.message);return}if(data.user&&data.session){const {error:pe}=await s.from('growmeal_profiles').insert({id:data.user.id,full_name:name,role:'learner',preferred_language:'en'});if(pe&&!pe.message.toLowerCase().includes('duplicate')){setMsg(pe.message);return}location.href='/hq';return}setMsg('Account created. Check your email to confirm your address, then sign in.');}
return <div className="auth"><img src="/brand/agrishine-logo.png" alt="AgriShine"/><h1>Create GrowMeal account</h1><p>Learner accounts begin with least-privilege access. School roles are assigned through school membership.</p><form onSubmit={go}><label>Full name<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="btn primary">Create account</button>{msg&&<p className="form-msg">{msg}</p>}</form><p><Link href="/login">Already registered? Sign in</Link></p></div>}