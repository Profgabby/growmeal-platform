import { PageHeader, StatusPill } from '@/components/ui'; import { weekSequence } from '@/lib/data';
export default function Week(){return <><PageHeader eyebrow="PRIMARY 4A · WEEK 6" title="Gravity Drip Garden" description="Investigate how gravity-fed drip irrigation distributes water across a school garden."/><div className="lessonMeta"><span>Garden GD-26</span><span>60–75 min</span><span>5 materials</span><span className="offlineReady">● Offline ready</span></div><div className="lessonSequence">{weekSequence.map(([n,title,detail,status])=><section className={`lessonStep ${status==='In progress'?'active':''}`} key={n}><div className="stepNo">{n}</div><div className="stepBody"><div className="stepTop"><h2>{title}</h2><StatusPill status={status==='Complete'?'good':status==='In progress'?'warning':'neutral'}>{status}</StatusPill></div><p>{detail}</p>{status==='In progress'&&<div className="inlineForm"><label>Observation note<textarea defaultValue="Emitter 3 produced visibly less water than emitter 1."/></label><button className="button">Save evidence</button></div>}</div></section>)}</div><div className="stickyActions"><button className="button secondary">Save draft</button><button className="button">Mark lesson delivered</button></div></>}
