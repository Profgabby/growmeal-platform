'use client';import Link from 'next/link';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');async function go(e:any){e.preventDefault();const s=createClient();const {error}=await s.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else location.href=new URLSearchParams(location.search).get('next')||'/hq'}return <div className="auth"><img src="/brand/agrishine-logo.png" alt="AgriShine"/><h1>Welcome to GrowMeal™</h1><p>Sign in to your AgriShine school workspace.</p><form onSubmit={go}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="btn primary">Sign in</button>{msg&&<p className="error">{msg}</p>}</form><p><Link href="/signup">Create a learner account</Link> · <Link href="/onboarding">Set up a school workspace</Link></p></div>}